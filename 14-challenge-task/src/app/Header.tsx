import Image from 'next/image';

const Header = () => {
  return (
    <div className='h-16 bg-primary' style={{ backgroundImage: 'url(/images/bg-header-desktop.svg)' }}>
    </div>
  );
}

export default Header;
